# `[serve]` Section

* `default` sets the default page to display when serving the documentation.
* `theme` specifies the theme directory for the server.
* `title` sets the title for the served documentation that appears in the browser.

```docdustry-docmeta
id: ini-serve-section
tag: user
```
